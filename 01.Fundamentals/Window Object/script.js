// Definition and Usage
// The alert() method displays an alert box with a message and an OK button.

// The alert() method is used when you want information to come through to the user.

window.alert("This is Alert Message!")

// Definition and Usage
// The prompt() method displays a dialog box that prompts the user for input.

// The prompt() method returns the input value if the user clicks "OK", otherwise it returns null.

let userinput = window.prompt("Enter a Number : ")

console.log(`The Number you Have Enter is ${userinput}`)